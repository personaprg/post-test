// import file system 
const fs = require('fs');
// import hyper text
const http = require('http');
const contentType = require("contentType");




let serve = http.createServer((req, res) => {
  if(req === METHODS)

});